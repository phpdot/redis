<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
         executionOrder="random">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <!-- Compose-stack defaults (docker-compose.yml shifts host ports off the local
         defaults); the env vars override for other environments. -->
    <php>
        <env name="REDIS_PORT" value="6380"/>
        <env name="REDIS_AUTH_PORT" value="6381"/>
        <env name="REDIS_AUTH_PASS" value="phpdot-secret"/>
    </php>
</phpunit>
